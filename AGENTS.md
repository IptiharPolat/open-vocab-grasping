# Project agent notes

- Keep oracle data paths explicitly labelled; never present simulator IDs as perception output.
- Keep every coordinate transform named `T_destination_source`.
- All default tests and `cpu_smoke.yaml` must run without CUDA or model weights.
- Record third-party sources, versions, and licenses in `docs/third_party_licenses.md`.
- Update `PROGRESS.md` after each verified stage.

